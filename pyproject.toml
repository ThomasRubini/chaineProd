[tool.poetry]
name = "tmp2"
version = "0.1.0"
description = ""
authors = ["Thomas Rubini"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pytest = "^7.4.3"
flask = "^3.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
